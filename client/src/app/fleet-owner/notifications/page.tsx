'use client';

import { NotificationCenter } from '@/components/worker/NotificationCenter';

export default function FleetOwnerNotificationsPage() {
  return <NotificationCenter accent="primary" />;
}
