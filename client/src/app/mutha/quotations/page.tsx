// Same screen for every service-providing role; this route puts it inside
// mutha's own layout so the role guard and nav apply.
export { default } from '@/components/pricing/WorkerQuotationsScreen';
